import { PageSlugs } from "@models/pages/enums";
import { PageData } from "@models/pages/types";

export const AdministratorPagesData: PageData = {
    ADMIN_PANEL: {
        name: "Панель администратора",
        slug: PageSlugs.ADMIN_PANEL
    }
};
