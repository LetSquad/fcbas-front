import { PageSlugs } from "@models/pages/enums";
import { PageData } from "@models/pages/types";

export const ExecutorPagesData: PageData = {
    DASHBOARD: {
        name: "Дашборд",
        slug: PageSlugs.DASHBOARD
    }
};
